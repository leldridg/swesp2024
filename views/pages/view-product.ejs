<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Product - <% item.name %></title>
    <link rel="stylesheet" href="/stylesheets/viewProduct.css">
</head>
<body>
    <div class="product-container">
        <div class="product-details">
            <h1><%= item.name %></h1>
            <div class="product-image">
                <img src="<%= item.image %>" style="max-width: 400px; height: auto;">
            </div>            
            <p class="price">$<%= item.price %></p>
            <% if (item.quantity > 0) { %>
                <p class="stock-status">Currently in Stock!</p>
            <% } else { %>
                <p class="stock-status">Out of Stock</p>
            <% } %>
            <div class="quantity-selector">
                <label for="quantity">Qty:</label>
                <select id="quantity" name="quantity">
                    <option value="1">1</option>
                    <option value="2">2</option>
                    <option value="3">3</option>
                    <!-- Adjust quantity options based on product availability or omit if not applicable -->
                </select>
            </div>
            <button class="add-to-cart-btn">Add to Cart</button>
            <p class="product-description"><%= item.description %></p>
        </div>
    </div>
</body>
</html>
